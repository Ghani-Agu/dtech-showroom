/**
 * ROUND 16 — customer accounts were removed (visitors subscribe via the
 * NewsletterPopup, which reuses newsletter-actions.subscribeAction; no
 * storefront accounts, zero web-app access). Empty stub because the device
 * mount cannot delete files — safe to delete from Windows/VS Code.
 */
export {}
