/**
 * ROUND 16 — customer accounts were removed (visitors subscribe via the
 * NewsletterPopup instead; no storefront accounts, zero web-app access).
 * This file is an empty stub because the device mount cannot delete files —
 * the src/components/account folder is safe to delete from Windows/VS Code.
 */
export {}
